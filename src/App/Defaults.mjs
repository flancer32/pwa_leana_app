export default class Fl32_Leana_App_Defaults {
    /**
     * Default port for listing.
     * @type {number}
     * @default
     */
    SERVER_DEFAULT_PORT = 3000;
    /**
     * PID file to stop running server.
     * @type {string}
     * @default
     */
    PID_FILE_NAME = './var/server.pid';
}
