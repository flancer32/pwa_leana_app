/**
 * Hardcoded configuration (constants).
 */
export default class Fl32_Leana_Shared_Const {
    // calendar related config (date picker)
    calendar = {
        minDate: 0,
        maxDate: 21,
    }

}
