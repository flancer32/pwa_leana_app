/**
 * Hardcoded configuration for the module.
 */
export default class Fl32_Leana_Defaults {

    ACL_IS_CUSTOMER = 'is_customer';
    ACL_IS_DEVELOPER = 'is_developer';
    ACL_IS_EMPLOYEE = 'is_employee';
    DI_SESSION = 'appSession';

    constructor() {
        Object.freeze(this);
    }
}
